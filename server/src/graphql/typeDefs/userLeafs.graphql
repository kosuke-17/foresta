# ユーザー習得技術情報
type UserLeafs {
  id: ID!
  myForest: [treeInfo!]!
  userId: ID!
}
type treeInfo {
  treeId: ID!
  areaId: ID!
  treeName: String!
  achievementRate: Int!
  branches: [branchInfo!]!
}
type branchInfo {
  name: String!
  leafs: [leafInfo!]!
}
# isStatusが!あるとエラー起こる
type leafInfo {
  name: String!
  techBranch_id: String!
  techTree_id: String!
  isStatus: Boolean
}

################
#リクエストボディ

# TechLeaf情報
input UserTechLeafUpdateInput {
  userLeafsId: ID!
  treeId: ID!
  leafId: ID!
  achievementRate: Int!
  currentStatus: Boolean!
}

################################
#レスポンス

# ユーザーの習得技術情報
type ResponseUserTechLeaf {
  status: String!
  node: UserLeafs!
  msg: String!
}
