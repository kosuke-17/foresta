export type PortfolioIdType = {
  portfolioId: string;
};
export type PortfolioType = {
  portfolio: {
    portfolioId?: string;
    title: string;
    description: string;
    img: string;
    portfolioURL: string;
    userId: string;
    specSheetId: string;
  };
};
